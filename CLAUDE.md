@README.md

NOTE: Always use `make sign` when testing locally. Otherwise the permissions prompts I get are too tedious to approve and I will likely reject them.

NOTE: Always use `make test` to run tests (not `go test ./...`).
